# 方舟（豆包）SDK
volcengine-python-sdk[ark]

# 从 .env 加载环境变量（ARK_API_KEY 等）
python-dotenv>=1.0.0

# OpenAI 兼容 SDK（可选，用于 openai 包调用方式）
openai>=1.0.0

# 天气 Function Calling 示例中请求 Open-Meteo API
requests>=2.28.0

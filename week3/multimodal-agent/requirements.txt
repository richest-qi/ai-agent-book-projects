# Core dependencies
google-genai>=0.1.0  # New Google AI SDK for Gemini
openai>=1.50.0
httpx>=0.27.0
python-dotenv>=1.0.0

# For async support (stdlib asyncio is built into Python 3.4+; aiofiles for async file I/O)
aiofiles>=24.1.0

# For file handling (mimetypes is stdlib — used in agent.py, do not pip install)
python-magic>=0.4.27

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0

# Optional: For Mistral OCR (PyPI package name is mistralai, not mistral-ai)
mistralai>=1.2.0
